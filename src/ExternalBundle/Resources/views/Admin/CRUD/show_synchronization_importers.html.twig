{% extends 'MMCSonataAdminBundle:CRUD:base_show_list_items.html.twig' %}

{% trans_default_domain 'Synchronization' %}
{% import 'ExternalBundle::macros.html.twig' as macros %}

{% block item %}
    {{ macros.progressGroup(
        item.id,
        ('options.class.'~item.name)|trans,
        item.progress,
        item.total,
        item.progressing,
        item.exceptions
    )}}
{% endblock item %}
